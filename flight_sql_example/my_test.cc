#include <arrow/flight/client.h>

namespace arrow {
namespace flight {
	TimeoutDuration td;
}
}

arrow::Status Main() {
	// ARROW_ASSIGN_OR_RAISE(auto location, arrow::flight::Location::ForGrpcTcp("localhost", 31337));
}

int main() {
	return 0;
}

